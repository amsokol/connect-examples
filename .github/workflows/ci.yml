name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Bazel
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Check out repository
        uses: actions/checkout@v7.0.1

      - name: Set up Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          bazelisk-version: 1.29.0
          disk-cache: ${{ github.workflow }}
          repository-cache: true
          bazelrc: |
            common --color=yes
            common --curses=no
            build --show_timestamps
            build --verbose_failures
            # ubuntu-latest is 24.04: AppArmor blocks unprivileged user
            # namespaces, so linux-sandbox dies with Bazel exit 36.
            build --spawn_strategy=processwrapper-sandbox
            test --test_output=errors

      - name: Check generated code is up to date
        run: bazel test //api/v1:generate_tests --test_output=all

      - name: Test
        run: bazel test //... --keep_going

      - name: Build
        run: bazel build //... --keep_going
