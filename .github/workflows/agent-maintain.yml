# Maintain — thin product wrapper; runner install via composite @v0.3.13
# Product toolchain stays here (Go / Python / Rust / Buf).
# Triggers: workflow_dispatch + issue-wake only (no push-to-main).

name: agent-maintain

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "dry-run (plan only) or ship (Issues / fix PRs / reconcile)"
        required: true
        type: choice
        options:
          - dry-run
          - ship
        default: dry-run
  issue_comment:
    types: [created]

concurrency:
  group: agent-maintain
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  agent-maintain:
    name: Agent maintain (main)
    if: >
      github.event_name == 'workflow_dispatch' ||
      (
        github.event_name == 'issue_comment' &&
        !github.event.issue.pull_request &&
        github.event.comment.user.type != 'Bot' &&
        contains(join(github.event.issue.labels.*.name, ','), 'agent')
      )
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
      AGENT_MAINTAIN_MODEL: ${{ vars.AGENT_MAINTAIN_MODEL || vars.CURSOR_AGENT_MODEL || vars.CURSOR_DEP_MODEL || vars.CURSOR_REV_MODEL || vars.CURSOR_VUL_MODEL }}
      AGENT_SCM: github
      GH_TOKEN: ${{ github.token }}
      BUF_TOKEN: ${{ secrets.BUF_TOKEN }}
      GIT_AUTHOR_NAME: agent
      GIT_AUTHOR_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
      GIT_COMMITTER_NAME: agent
      GIT_COMMITTER_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
      AGENT_WAKE_ISSUE: ${{ github.event.issue.number }}
      AGENT_WAKE_COMMENT: ${{ github.event.comment.body }}
    steps:
      - name: Check secrets and vars
        run: |
          set -euo pipefail
          if [ -z "${CURSOR_API_KEY:-}" ]; then
            echo "::error::Missing secret CURSOR_API_KEY"
            exit 1
          fi
          if [ -z "${AGENT_MAINTAIN_MODEL:-}" ]; then
            echo "::error::Missing variable AGENT_MAINTAIN_MODEL (or CURSOR_*_MODEL fallback)"
            exit 1
          fi
          echo "AGENT_MAINTAIN_MODEL=$AGENT_MAINTAIN_MODEL wake_issue=${AGENT_WAKE_ISSUE:-none}"

      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: true
          fetch-depth: 0
          token: ${{ secrets.AGENT_WORKFLOW_TOKEN || github.token }}

      - name: Configure git
        run: |
          set -euo pipefail
          git config user.name "$GIT_AUTHOR_NAME"
          git config user.email "$GIT_AUTHOR_EMAIL"

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Download modules and install Go tools
        run: |
          set -euo pipefail
          go mod download
          go install tool

      - name: Install agent runner
        id: runner
        uses: amsokol/ai-devsecops-cursor/.github/actions/install-agent-runner@v0.3.13

      - name: Agent-maintain dry-run
        if: github.event_name == 'workflow_dispatch' && inputs.mode == 'dry-run'
        working-directory: ${{ steps.runner.outputs.path }}
        env:
          TARGET_PATH: ${{ github.workspace }}
          TARGET_ID: connect-examples
        run: uv run agent-maintain --dry-run

      - name: Agent-maintain ship
        if: >
          github.event_name == 'issue_comment' ||
          (github.event_name == 'workflow_dispatch' && inputs.mode == 'ship')
        working-directory: ${{ steps.runner.outputs.path }}
        env:
          TARGET_PATH: ${{ github.workspace }}
          TARGET_ID: connect-examples
        run: uv run agent-maintain
