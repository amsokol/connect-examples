# Workspace buf config. Generate/lint copy this file; BSR deps stay remote.
version: v2
modules:
  - path: .
    includes:
      - api
deps:
  - buf.build/bufbuild/protovalidate:v1.2.2
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
