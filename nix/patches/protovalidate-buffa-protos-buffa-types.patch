--- a/crates/protovalidate-buffa-protos/Cargo.toml
+++ b/crates/protovalidate-buffa-protos/Cargo.toml
@@ -16,6 +16,8 @@
 
 [dependencies]
 buffa = "0.8"
+buffa-types = "0.8"
+buffa-descriptor = "0.8"
 
 [build-dependencies]
 buffa-build = "0.8"
--- a/crates/protovalidate-buffa-protos/src/lib.rs
+++ b/crates/protovalidate-buffa-protos/src/lib.rs
@@ -20,3 +20,7 @@
 )]
 
 include!(concat!(env!("OUT_DIR"), "/_include.rs"));
+
+// buffa-codegen maps google.protobuf.* to ::buffa_types; re-export so the
+// plugin can keep using protovalidate_buffa_protos::google::protobuf::*.
+pub use buffa_types::google;
