diff --git a/Cargo.toml b/Cargo.toml
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -16,6 +16,8 @@ workspace = true
 
 [dependencies]
 buffa = "0.8"
+buffa-types = "0.8"
+buffa-descriptor = "0.8"
 
 [build-dependencies]
 buffa-build = "0.8"
diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -17,4 +17,8 @@
     reason = "generated code may emit unused-imports/dead-code warnings"
 )]
 
 include!(concat!(env!("OUT_DIR"), "/_include.rs"));
+
+// buffa-codegen maps google.protobuf.* to ::buffa_types; re-export so the
+// plugin can keep using protovalidate_buffa_protos::google::protobuf::*.
+pub use buffa_types::google;
diff --git a/build.rs b/build.rs
--- a/build.rs
+++ b/build.rs
@@ -28,20 +28,7 @@
     }
     let includes_str: Vec<&str> = includes.iter().map(String::as_str).collect();
 
-    // validate.proto transitively uses FieldDescriptorProto.Type, so compile
-    // all required google.protobuf WKTs locally alongside it.
-    let mut files = vec![validate_proto.to_str().expect("utf-8 path").to_string()];
-    if let Some(ref inc) = protoc_include {
-        for wkt in &[
-            "google/protobuf/descriptor.proto",
-            "google/protobuf/duration.proto",
-            "google/protobuf/field_mask.proto",
-            "google/protobuf/timestamp.proto",
-        ] {
-            let p = inc.join(wkt);
-            if p.exists() {
-                files.push(p.to_str().expect("utf-8 path").to_string());
-            }
-        }
-    }
+    // Compile only validate.proto. WKTs are protoc includes so imports resolve;
+    // buffa-codegen maps google.protobuf.* to ::buffa_types.
+    let files = vec![validate_proto.to_str().expect("utf-8 path").to_string()];
     let files_str: Vec<&str> = files.iter().map(String::as_str).collect();
