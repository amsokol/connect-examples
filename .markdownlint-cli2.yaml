# markdownlint-cli2 config (https://github.com/DavidAnson/markdownlint-cli2)
config:
  default: true
  # READMEs often wrap poorly in tables / long URLs.
  MD013: false
  # Allow HTML in docs if needed later.
  MD033: false

globs:
  - "**/*.md"

ignores:
  - ".venv/**"
  - "result*/**"
  - "**/node_modules/**"
